<% unless current_user.nil? %>
	<h1>Upload a Photo</h1>

	<%= form_for(@photo, :html => { :multipart => true }) do |f| %>
		<%= f.file_field :image %>
		<%= f.submit "Upload" %>
	<% end %>

<% else %>
	<%= render "signup_form" %>
<% end %>

<!-- render "signup_form" -->